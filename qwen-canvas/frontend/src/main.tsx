import React,{useRef,useState}from"react";import{createRoot}from"react-dom/client";import{Stage,Layer,Image as KImage}from"react-konva";import"./style.css";const API="http://127.0.0.1:8000";type N={id:string,img:HTMLImageElement,x:number,y:number,w:number,h:number,file?:File};
function App(){const[nodes,setNodes]=useState<N[]>([]),[sel,setSel]=useState<string[]>([]),[prompt,setPrompt]=useState(""),[busy,setBusy]=useState(false),[mode,setMode]=useState("generate"),[ratio,setRatio]=useState("1:1"),[quality,setQuality]=useState("preview"),[steps,setSteps]=useState(30),[seed,setSeed]=useState(-1),[transparent,setTransparent]=useState(false),[director,setDirector]=useState("none"),[scale,setScale]=useState(1),[pos,setPos]=useState({x:0,y:0}),[project,setProject]=useState("我的工程");const input=useRef<HTMLInputElement>(null);const ratios:any={"1:1":[1,1],"3:4":[3,4],"4:3":[4,3],"16:9":[16,9],"9:16":[9,16]};const size=()=>{let[a,b]=ratios[ratio],base=quality==="2k"?2048:1024;return a>=b?[Math.round(base*a/b/32)*32,base]:[base,Math.round(base*b/a/32)*32]};
const addFile=(file:File)=>{let u=URL.createObjectURL(file),img=new Image();img.onload=()=>setNodes(n=>[...n,{id:crypto.randomUUID(),img,x:100+n.length*35,y:100+n.length*35,w:Math.min(img.width,640),h:Math.min(img.width,640)*img.height/img.width,file}]);img.src=u};
const enhance=async()=>{if(director==="none"||!prompt.trim())return;setBusy(true);try{let f=new FormData();f.append("provider",director);f.append("prompt",prompt);let j=await(await fetch(API+"/api/enhance",{method:"POST",body:f})).json();if(j.error)throw Error(j.error);setPrompt(j.prompt)}catch(e:any){alert(e.message)}finally{setBusy(false)}};
const generate=async()=>{if(!prompt.trim())return;setBusy(true);try{let f=new FormData(),[w,h]=size();Object.entries({prompt,mode,width:w,height:h,steps,seed,transparent}).forEach(([k,v])=>f.append(k,String(v)));if(mode==="edit")nodes.filter(n=>sel.includes(n.id)).slice(0,10).forEach(n=>n.file&&f.append("images",n.file));let j=await(await fetch(API+"/api/generate",{method:"POST",body:f})).json();if(j.error)throw Error(j.error);addFile(new File([await(await fetch(API+j.url)).blob()],"qwen-result.png",{type:"image/png"}));setSeed(j.seed)}catch(e:any){alert(e.message)}finally{setBusy(false)}};
const saveProject=async()=>{let data={prompt,mode,ratio,quality,steps,seed,transparent,director,canvas:{scale,pos},nodes:nodes.map(n=>({x:n.x,y:n.y,w:n.w,h:n.h,name:n.file?.name}))};let r=await fetch(API+"/api/projects/"+encodeURIComponent(project),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});alert((await r.json()).file?"工程已保存":"保存失败")};
return <div className="app"><header><b>Qwen Canvas</b><span>RTX 4080 16G · Local First</span><input className="project" value={project} onChange={e=>setProject(e.target.value)}/><button onClick={saveProject}>保存工程</button><i>{sel.length} 个已选</i></header><aside className="tools"><button onClick={()=>input.current?.click()}>＋图片</button><input ref={input} hidden type="file" multiple accept="image/*" onChange={e=>[...(e.target.files||[])].forEach(addFile)}/><button onClick={()=>setSel([])}>取消选中</button><button onClick={()=>{setNodes([]);setSel([])}}>清空</button></aside><main><Stage width={window.innerWidth-390} height={window.innerHeight-52} draggable x={pos.x} y={pos.y} scaleX={scale} scaleY={scale} onDragEnd={e=>setPos({x:e.target.x(),y:e.target.y()})} onWheel={e=>{e.evt.preventDefault();setScale(s=>Math.max(.15,Math.min(4,s*(e.evt.deltaY>0?.9:1.1))))}}><Layer>{nodes.map(n=><KImage key={n.id} image={n.img} x={n.x} y={n.y} width={n.w} height={n.h} draggable stroke={sel.includes(n.id)?"white":undefined} strokeWidth={sel.includes(n.id)?3:0} onClick={e=>{e.cancelBubble=true;setSel(s=>e.evt.shiftKey?(s.includes(n.id)?s.filter(x=>x!==n.id):[...s,n.id]):[n.id])}}/>)}</Layer></Stage></main><section className="panel"><h3>AI 创作</h3><div className="tabs"><button className={mode==="generate"?"on":""} onClick={()=>setMode("generate")}>文生图</button><button className={mode==="edit"?"on":""} onClick={()=>setMode("edit")}>图片编辑</button></div><label>Prompt 导演</label><select value={director} onChange={e=>setDirector(e.target.value)}><option value="none">关闭 / 原始提示词</option><option value="openai">GPT · OpenAI API</option><option value="gemini">Gemini API</option></select><label>提示词</label><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="输入中文即可…"/><button className="secondary" disabled={busy||director==="none"} onClick={enhance}>✦ AI 优化提示词</button><div className="row"><div><label>比例</label><select value={ratio} onChange={e=>setRatio(e.target.value)}>{Object.keys(ratios).map(x=><option key={x}>{x}</option>)}</select></div><div><label>质量</label><select value={quality} onChange={e=>setQuality(e.target.value)}><option value="preview">预览 1K</option><option value="2k">原生 2K</option></select></div></div><label>输出：{size()[0]} × {size()[1]}</label><label>Steps: {steps}</label><input type="range" min="10" max="50" value={steps} onChange={e=>setSteps(+e.target.value)}/><label>Seed</label><input className="seed" type="number" value={seed} onChange={e=>setSeed(+e.target.value)}/><label className="check"><input type="checkbox" checked={transparent} onChange={e=>setTransparent(e.target.checked)}/> 原生透明 RGBA</label><div className="hint">{mode==="edit"?`参考图 ${Math.min(sel.length,10)}/10`:"Qwen 本地生成"} · 2K 会明显增加显存/耗时</div><button className="generate" disabled={busy} onClick={generate}>{busy?"处理中…":"✦ 开始生成"}</button></section></div>};createRoot(document.getElementById("root")!).render(<App/>);
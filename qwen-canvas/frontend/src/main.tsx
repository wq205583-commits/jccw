import React,{useEffect,useRef,useState}from"react";import{createRoot}from"react-dom/client";import{Stage,Layer,Image as KImage,Rect,Transformer,Line}from"react-konva";import"./style.css";const API="http://127.0.0.1:8000";type N={id:string,img:HTMLImageElement,x:number,y:number,w:number,h:number,rotation?:number,file?:File,parentId?:string,isResult?:boolean};type Box={x:number,y:number,w:number,h:number}|null;
function App(){const[nodes,setNodes]=useState<N[]>([]),[sel,setSel]=useState<string[]>([]),[prompt,setPrompt]=useState(""),[busy,setBusy]=useState(false),[mode,setMode]=useState("generate"),[tool,setTool]=useState("select"),[box,setBox]=useState<Box>(null),[ratio,setRatio]=useState("1:1"),[quality,setQuality]=useState("preview"),[steps,setSteps]=useState(30),[seed,setSeed]=useState(-1),[transparent,setTransparent]=useState(false),[director,setDirector]=useState("none"),[scale,setScale]=useState(1),[pos,setPos]=useState({x:0,y:0}),[project,setProject]=useState("我的工程"),[menu,setMenu]=useState<{x:number,y:number,id:string}|null>(null),[maskLines,setMaskLines]=useState<number[][]>([]),[brush,setBrush]=useState(36),[refs,setRefs]=useState<string[]>([]),[refRoles,setRefRoles]=useState<Record<string,string>>({});const input=useRef<HTMLInputElement>(null),refInput=useRef<HTMLInputElement>(null),drag=useRef<any>(null),tr=useRef<any>(null);const ratios:any={"1:1":[1,1],"3:4":[3,4],"4:3":[4,3],"3:2":[3,2],"2:3":[2,3],"16:9":[16,9],"9:16":[9,16]};const sizes:any={preview:{"1:1":[1024,1024],"4:3":[1200,896],"3:4":[896,1200],"3:2":[1264,848],"2:3":[848,1264],"16:9":[1376,768],"9:16":[768,1376]},"2k":{"1:1":[2048,2048],"4:3":[2400,1792],"3:4":[1792,2400],"3:2":[2528,1696],"2:3":[1696,2528],"16:9":[2752,1536],"9:16":[1536,2752]}};const size=()=>sizes[quality][ratio];
useEffect(()=>{let t=tr.current;if(!t)return;let stage=t.getStage();let node=sel.length===1?stage?.findOne("#img-"+sel[0]):null;t.nodes(node?[node]:[]);t.getLayer()?.batchDraw()},[sel,nodes,tool]);const updateNode=(id:string,p:Partial<N>)=>setNodes(ns=>ns.map(n=>n.id===id?{...n,...p}:n));const addFile=(file:File,asRef=false,parentId?:string)=>{let u=URL.createObjectURL(file),img=new Image();img.onload=()=>{let id=crypto.randomUUID();setNodes(ns=>{let parent=parentId?ns.find(x=>x.id===parentId):null,w=Math.min(img.width,640),h=Math.min(img.width,640)*img.height/img.width;return [...ns,{id,img,x:parent?parent.x+parent.w+36:100+ns.length*35,y:parent?parent.y:100+ns.length*35,w,h,file,parentId,isResult:!!parentId}]});if(asRef){setRefs(v=>[...v,id].slice(0,9));setRefRoles(v=>({...v,[id]:"参考"}))}};img.src=u};
const enhance=async()=>{if(director==="none"||!prompt.trim())return;setBusy(true);try{let f=new FormData();f.append("provider",director);f.append("prompt",prompt);let j=await(await fetch(API+"/api/enhance",{method:"POST",body:f})).json();if(j.error)throw Error(j.error);setPrompt(j.prompt)}catch(e:any){alert(e.message)}finally{setBusy(false)}};
const makeMaskRef=async()=>{let n=nodes.find(x=>sel.includes(x.id));if(!n||!maskLines.length)return null;let cv=document.createElement("canvas");cv.width=n.img.naturalWidth;cv.height=n.img.naturalHeight;let ctx=cv.getContext("2d")!;ctx.drawImage(n.img,0,0,cv.width,cv.height);let sx=cv.width/n.w,sy=cv.height/n.h;ctx.strokeStyle="rgba(255,0,0,.85)";ctx.lineCap="round";ctx.lineJoin="round";for(let pts of maskLines){ctx.beginPath();for(let i=0;i<pts.length;i+=2){let x=(pts[i]-n.x)*sx,y=(pts[i+1]-n.y)*sy;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.lineWidth=brush*Math.max(sx,sy);ctx.stroke()}return await new Promise<File>(ok=>cv.toBlob(b=>ok(new File([b!],"qwen-mask-annotation.png",{type:"image/png"})),"image/png"))};const generate=async()=>{if(!prompt.trim())return;setBusy(true);try{let finalPrompt=prompt;if(mode!=="generate"&&refs.length){let roles=refs.map((id,i)=>`<image${i+2}> 用作${refRoles[id]||"参考"}`).join("；");finalPrompt+=`. Reference roles: <image1> is the primary image；${roles}. Follow these image roles strictly.`;}if(tool==="region"&&box)finalPrompt+=`. Only modify the marked rectangular region (x=${Math.round(box.x)}, y=${Math.round(box.y)}, width=${Math.round(box.w)}, height=${Math.round(box.h)}); keep everything outside unchanged.`;if(tool==="outpaint")finalPrompt+=`. Extend the scene naturally beyond the original image boundaries, preserving the original image content and visual continuity.`;if(tool==="mask"&&maskLines.length)finalPrompt+=`. <image1> is the original image. <image2> is the same image with red painted annotations. Edit only the red-painted region shown in <image2>; preserve everything outside that region exactly.`;let f=new FormData(),[w,h]=size();Object.entries({prompt:finalPrompt,mode:mode==="generate"?"generate":"edit",width:w,height:h,steps,seed,transparent}).forEach(([k,v])=>f.append(k,String(v)));if(mode!=="generate")nodes.filter(n=>sel.includes(n.id)).forEach(n=>n.file&&f.append("images",n.file));nodes.filter(n=>refs.includes(n.id)&&!sel.includes(n.id)).slice(0,9).forEach(n=>n.file&&f.append("images",n.file));if(tool==="mask"){let maskRef=await makeMaskRef();if(maskRef)f.append("images",maskRef)}let j=await(await fetch(API+"/api/generate",{method:"POST",body:f})).json();if(j.error)throw Error(j.error);addFile(new File([await(await fetch(API+j.url)).blob()],"qwen-result.png",{type:"image/png"}),false,sel[0]);setSeed(j.seed)}catch(e:any){alert(e.message)}finally{setBusy(false)}};
const pointer=(e:any)=>{let p=e.target.getStage().getRelativePointerPosition();return p};const down=(e:any)=>{if(tool==="mask"){let p=pointer(e);drag.current=true;setMaskLines(v=>[...v,[p.x,p.y]]);return}if(tool!=="region")return;let p=pointer(e);drag.current=p;setBox({x:p.x,y:p.y,w:1,h:1})};const move=(e:any)=>{if(tool==="mask"&&drag.current){let p=pointer(e);setMaskLines(v=>{let a=[...v],i=a.length-1;a[i]=[...a[i],p.x,p.y];return a});return}if(tool!=="region"||!drag.current)return;let p=pointer(e),s=drag.current;setBox({x:Math.min(s.x,p.x),y:Math.min(s.y,p.y),w:Math.abs(p.x-s.x),h:Math.abs(p.y-s.y)})};const up=()=>{drag.current=null};
const replaceParent=(id:string)=>setNodes(ns=>{let result=ns.find(x=>x.id===id),parent=result?.parentId?ns.find(x=>x.id===result.parentId):null;if(!result||!parent)return ns;return ns.map(x=>x.id===parent.id?{...x,img:result.img,file:result.file}:x).filter(x=>x.id!==id)});const duplicate=(id:string)=>setNodes(ns=>{let n=ns.find(x=>x.id===id);return n?[...ns,{...n,id:crypto.randomUUID(),x:n.x+30,y:n.y+30}]:ns});const downloadNode=(id:string)=>{let n=nodes.find(x=>x.id===id);if(!n?.file)return;let a=document.createElement("a");a.href=URL.createObjectURL(n.file);a.download=n.file.name||"qwen-image.png";a.click()};const chooseAction=(action:string)=>{if(!menu)return;setSel([menu.id]);setMenu(null);setMode("edit");setBox(null);if(action==="edit"){setTool("select");setPrompt("")}if(action==="remove"){setTool("mask");setMaskLines([]);setPrompt("删除红色涂抹标记覆盖的主体，并根据周围环境自然补全背景，保持原图其他内容不变")}if(action==="background"){setTool("select");setPrompt("只更换背景，保持主体人物或产品的身份、外观、姿势和细节不变")}if(action==="outpaint"){setTool("outpaint");setPrompt("自然扩展画面边界，保持原图内容和视觉连续性")}if(action==="transparent"){setTool("select");setTransparent(true);setPrompt("移除背景，只保留主体，输出透明背景 RGBA PNG")}if(action==="style"){setTool("select");setPrompt("保持主体身份和构图不变，按照以下要求进行风格转换：")}if(action==="enhance"){setTool("select");setPrompt("高清修复并增强细节与清晰度，不改变人物身份、五官、构图和原始设计")}if(action==="clothes"){setTool("select");setPrompt("只更换人物服装，保持人物身份、脸部、发型、姿势和背景不变。新服装：")}if(action==="duplicate"){duplicate(menu.id)}if(action==="download"){downloadNode(menu.id)}if(action==="replace"){replaceParent(menu.id);setSel([])}if(action==="delete"){setNodes(n=>n.filter(x=>x.id!==menu.id));setSel([])}};const saveProject=async()=>{let data={prompt,mode,tool,box,ratio,quality,steps,seed,transparent,director,canvas:{scale,pos},nodes:nodes.map(n=>({x:n.x,y:n.y,w:n.w,h:n.h,name:n.file?.name}))};let r=await fetch(API+"/api/projects/"+encodeURIComponent(project),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});alert((await r.json()).file?"工程已保存":"保存失败")};
return <div className="app"><header><b>Qwen Canvas</b><span>RTX 4080 16G · Local First</span><input className="project" value={project} onChange={e=>setProject(e.target.value)}/><button onClick={saveProject}>保存工程</button><i>{sel.length} 个已选</i></header><aside className="tools"><button className={tool==="select"?"active":""} onClick={()=>setTool("select")}>↖ 选择</button><button className={tool==="region"?"active":""} onClick={()=>{setTool("region");setMode("edit")}}>▣ 框选</button><button className={tool==="outpaint"?"active":""} onClick={()=>{setTool("outpaint");setMode("edit")}}>⤢ 扩图</button><button onClick={()=>input.current?.click()}>＋图片</button><input ref={input} hidden type="file" multiple accept="image/*" onChange={e=>[...(e.target.files||[])].forEach(addFile)}/><button onClick={()=>{setBox(null);setSel([])}}>取消</button></aside><main onContextMenu={e=>e.preventDefault()}><Stage width={window.innerWidth-390} height={window.innerHeight-52} draggable={tool!=="region"&&tool!=="mask"} x={pos.x} y={pos.y} scaleX={scale} scaleY={scale} onDblClick={e=>{if(e.target===e.target.getStage())input.current?.click()}} onMouseDown={down} onMouseMove={move} onMouseUp={up} onDragEnd={e=>{if(e.target===e.target.getStage())setPos({x:e.target.x(),y:e.target.y()})}} onWheel={e=>{e.evt.preventDefault();setScale(s=>Math.max(.15,Math.min(4,s*(e.evt.deltaY>0?.9:1.1))))}}><Layer>{nodes.map(n=><KImage key={n.id} id={"img-"+n.id} image={n.img} x={n.x} y={n.y} width={n.w} height={n.h} rotation={n.rotation||0} draggable={tool==="select"} onDragEnd={e=>updateNode(n.id,{x:e.target.x(),y:e.target.y()})} onTransformEnd={e=>{let o=e.target,sx=o.scaleX(),sy=o.scaleY();updateNode(n.id,{x:o.x(),y:o.y(),w:Math.max(20,o.width()*sx),h:Math.max(20,o.height()*sy),rotation:o.rotation()});o.scaleX(1);o.scaleY(1)}} stroke={sel.includes(n.id)?"white":undefined} strokeWidth={sel.includes(n.id)?3:0} onClick={e=>{e.cancelBubble=true;setSel([n.id]);setMenu(null)}} onContextMenu={e=>{e.evt.preventDefault();e.cancelBubble=true;setSel([n.id]);setMenu({x:e.evt.clientX,y:e.evt.clientY,id:n.id})}}/>)}{maskLines.map((pts,i)=><Line key={"mask"+i} points={pts} stroke="rgba(255,55,55,.65)" strokeWidth={brush/scale} lineCap="round" lineJoin="round" listening={false}/>)}{sel.length===1&&tool==="select"&&<Transformer ref={tr} rotateEnabled={true} keepRatio={true} flipEnabled={false} borderStroke="#fff" anchorStroke="#fff" anchorFill="#202735" anchorSize={9} boundBoxFunc={(oldBox,newBox)=>newBox.width<30||newBox.height<30?oldBox:newBox}/>} {box&&<Rect x={box.x} y={box.y} width={box.w} height={box.h} stroke="white" dash={[10,6]} strokeWidth={2/scale}/>}</Layer></Stage></main><section className="panel"><h3>AI 创作</h3><div className="tabs"><button className={mode==="generate"?"on":""} onClick={()=>{setMode("generate");setTool("select")}}>文生图</button><button className={mode==="edit"?"on":""} onClick={()=>setMode("edit")}>图片编辑</button></div><label>Prompt 导演</label><select value={director} onChange={e=>setDirector(e.target.value)}><option value="none">关闭 / 原始提示词</option><option value="openai">GPT · OpenAI API</option><option value="gemini">Gemini API</option></select><label>提示词</label><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder={tool==="region"?"框选区域后输入：删除人物，只保留背景…":tool==="outpaint"?"选择图片，描述需要向外延展的场景…":"输入中文即可…"}/><button className="secondary" disabled={busy||director==="none"} onClick={enhance}>✦ AI 优化提示词</button><div className="row"><div><label>比例</label><select value={ratio} onChange={e=>setRatio(e.target.value)}>{Object.keys(ratios).map(x=><option key={x}>{x}</option>)}</select></div><div><label>质量</label><select value={quality} onChange={e=>setQuality(e.target.value)}><option value="preview">预览 1K</option><option value="2k">原生 2K</option></select></div></div><label>输出：{size()[0]} × {size()[1]}</label><label>Steps: {steps}</label><input type="range" min="10" max="50" value={steps} onChange={e=>setSteps(+e.target.value)}/><label>Seed</label><input className="seed" type="number" value={seed} onChange={e=>setSeed(+e.target.value)}/><label className="check"><input type="checkbox" checked={transparent} onChange={e=>setTransparent(e.target.checked)}/> 原生透明 RGBA</label>{tool==="mask"&&<><label>Mask 画笔：{brush}px</label><input type="range" min="8" max="120" value={brush} onChange={e=>setBrush(+e.target.value)}/><button className="secondary" onClick={()=>setMaskLines([])}>清除 Mask</button></>}<div className="hint">{tool==="mask"?`Mask 涂抹 ${maskLines.length} 笔 · 在要修改的位置涂红色`:tool==="region"&&box?`局部区域 ${Math.round(box.w)}×${Math.round(box.h)}`:tool==="outpaint"?"扩图模式：保持原图并自然延展":mode==="edit"?`参考图 ${Math.min(sel.length,10)}/10`:"Qwen 本地生成"}</div><button className="generate" disabled={busy} onClick={generate}>{busy?"处理中…":"✦ 开始生成"}</button></section>{sel.length===1&&<div className="context-toolbar"><button onClick={()=>{setMode("edit");setTool("select");setPrompt("")}}>✦ 智能改图</button><button onClick={()=>{setMode("edit");setTool("outpaint");setPrompt("自然扩展画面边界，保持原图内容和视觉连续性")}}>⤢ 扩图</button><button onClick={()=>{setMode("edit");setTool("select");setPrompt("高清修复并增强细节与清晰度，不改变人物身份、五官、构图和原始设计")}}>▣ 智能超清</button><button onClick={()=>{setMode("edit");setTool("select");setTransparent(true);setPrompt("移除背景，只保留主体，输出透明背景 RGBA PNG")}}>♙ 抠图</button><button onClick={()=>{setMode("edit");setTool("select");setPrompt("基于当前主体生成不同视角，保持主体身份、服装、产品特征和视觉风格一致。视角：")}}>◉ 多角度</button><button onClick={e=>{let n=nodes.find(n=>n.id===sel[0]);if(n)setMenu({x:(e.currentTarget as HTMLElement).getBoundingClientRect().left,y:(e.currentTarget as HTMLElement).getBoundingClientRect().bottom+6,id:n.id})}}>⌘ 工具⌄</button></div>}{sel.length===1&&<div className="floating-editor"><div className="editor-ref">{nodes.find(n=>n.id===sel[0])&&<img title="主图" src={nodes.find(n=>n.id===sel[0])!.img.src}/>} {refs.map(id=>{let n=nodes.find(x=>x.id===id);return n?<span className="ref-chip" key={id}><img src={n.img.src}/><select value={refRoles[id]||"参考"} onChange={e=>setRefRoles(v=>({...v,[id]:e.target.value}))}><option>参考</option><option>人物</option><option>服装</option><option>背景</option><option>风格</option><option>产品</option><option>构图</option></select><button onClick={()=>setRefs(v=>v.filter(x=>x!==id))}>×</button></span>:null})}<button title="添加参考图" onClick={()=>refInput.current?.click()}>＋</button><input ref={refInput} hidden type="file" multiple accept="image/*" onChange={e=>[...(e.target.files||[])].forEach(f=>addFile(f,true))}/></div><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="描述你想对图片做什么，例如：删除人物和服饰，只保留图片背景"/><div className="editor-bottom"><span>Qwen-Image-2.1</span><span>{quality==="2k"?"2K":"1K"} · {ratio} · {1+refs.length}/10 图</span><button disabled={busy||!prompt.trim()} onClick={generate}>{busy?"…":"↑"}</button></div></div>}{menu&&<div className="image-menu" style={{left:menu.x,top:menu.y}} onMouseLeave={()=>setMenu(null)}><b>右键 · 图片处理</b><button onClick={()=>chooseAction("edit")}>✦ AI 编辑</button><button onClick={()=>chooseAction("remove")}>⌫ 删除人物 / 物体</button><button onClick={()=>chooseAction("background")}>▧ 更换背景</button><button onClick={()=>chooseAction("outpaint")}>⤢ 扩图</button><button onClick={()=>chooseAction("transparent")}>◫ 抠图 / 透明背景</button><button onClick={()=>chooseAction("style")}>✧ 风格转换</button><button onClick={()=>chooseAction("clothes")}>♙ 更换服装</button><button onClick={()=>chooseAction("enhance")}>⌁ 高清修复</button><hr/><button onClick={()=>chooseAction("duplicate")}>⧉ 复制图片</button><button onClick={()=>chooseAction("download")}>⇩ 导出原图</button>{nodes.find(n=>n.id===menu.id)?.isResult&&<><hr/><button onClick={()=>chooseAction("replace")}>✓ 替换原图</button></>}<hr/><button className="danger" onClick={()=>chooseAction("delete")}>删除画布图片</button></div>}</div>};createRoot(document.getElementById("root")!).render(<App/>);